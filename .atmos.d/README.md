# Atmos Configuration Extensions

Custom Atmos configuration that is automatically merged with `atmos.yaml`.

## Custom Commands

- `atmos up` - Start the application locally using Docker Compose
- `atmos down` - Stop the local Docker Compose application
