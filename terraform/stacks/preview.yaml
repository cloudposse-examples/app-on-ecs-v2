import:
  - _default.yaml
  - defaults/app.yaml
  - deps/*

vars:
  stage: preview

terraform:
  auth:
    identities:
      plat-dev/terraform:
        default: true
  
components:
  terraform:
    app:
      metadata:
        # Override Terraform workspace
        terraform_workspace: 'atmos-native-ci-{{ .atmos_stack }}-{{ .atmos_component }}-{{ env "PR_NUMBER" | default "0" }}'
      vars:
        attributes:
          - '{{ env "PR_NUMBER" | default "0" }}'
        service:
          enable_execute_command: true
